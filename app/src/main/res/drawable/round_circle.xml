<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Base gradient layer -->
    <item>
        <shape android:shape="oval">
            <gradient
                android:angle="0"
                android:centerColor="@color/actionBar"
                android:endColor="@color/actionBar"
                android:startColor="@color/actionBar"
                android:type="linear" />
            <corners android:radius="8dp" /> <!-- Radius for rounded corners -->
        </shape>
    </item>

</layer-list>
